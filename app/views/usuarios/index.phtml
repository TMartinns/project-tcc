<?php
echo (isset($this->alert)) ? $this->alert : '';

$this->setPartialsDir('partials' . DS . 'usuarios');
$this->partial('navTabs');
?>

<div class="tab-content">
    <div class="tab-pane fade show active" id="listarUsuarios" role="tabpanel" aria-labelledby="listarUsuariosTab">
        <br/>

        <div class="row">
            <?php
            $usuarios = Usuario::all();
            foreach ($usuarios as $usuario) {
                $pessoa = Pessoa::find_by_id($usuario->id_pessoa);

                $telefones = Telefone::find_all_by_id_pessoa($pessoa->id);

                $enderecos = Endereco::find_all_by_id_pessoa($pessoa->id);
                ?>
                <div class="col-md-4 col-lg-3">
                    <div class="card">
                        <img class="card-img-top fotoUsuario" src="<?php echo IMG; ?>" height="180">

                        <div class="card-body">
                            <h5 class="card-title"><?php echo $pessoa->nome; ?></h5>

                            <p class="card-text"><h6>Nome de usuário</h6><?php echo $usuario->nome_usuario; ?></p>

                            <p class="card-text"><h6>E-mail</h6><?php echo $usuario->email; ?></p>

                            <p class="card-text"><h6>
                                Função</h6><?php echo ($usuario->funcao == 'C') ? 'Coordenador' : 'Oficial de promotoria'; ?>
                            </p>

                            <div id="dadosPessoais<?php echo $pessoa->id; ?>" class="d-none">
                                <p class="card-text"><h6>CPF</h6><?php echo $pessoa->cpf; ?></p>

                                <p class="card-text"><h6>Data de
                                    nascimento</h6><?php echo $pessoa->data_nascimento; ?></p>

                                <p class="card-text"><h6>Nome da mãe</h6><?php echo $pessoa->nome_mae; ?></p>

                                <p class="card-text"><h6>Telefones</h6>
                                <?php
                                foreach ($telefones as $key => $telefone) {
                                    $hr = ($key != count($telefones) - 1) ? "<hr/>" : '';
                                    echo "<p class='card-text'> ($telefone->ddd) $telefone->numero</p>$hr";
                                }
                                ?>
                                </p>

                                <p class="card-text"><h6>Endereços</h6>
                                <?php
                                foreach ($enderecos as $key => $endereco) {
                                    $cidade = Cidade::find_by_id($endereco->id_cidade);

                                    $uf = Uf::find_by_id($cidade->id_uf);

                                    $hr = ($key != count($telefones) - 1) ? "<hr/>" : '';
                                    echo "<p class='card-text'> 
                                          $endereco->logradouro, $endereco->numero, $endereco->complemento, $endereco->bairro
                                          <br/>
                                          $cidade->nome/$uf->uf
                                          <br/>
                                          $endereco->cep
                                          </p>
                                          $hr";
                                }
                                ?>
                                </p>
                            </div>

                            <div class="text-center">
                                <button class="btn btn-outline-dark dadosPessoaisAppear" data-id="<?php echo $pessoa->id; ?>">
                                    <span><i class="fas fa-arrow-circle-down"></i></span>
                                </button>
                            </div>
                        </div>

                        <div class="card-footer text-center">
                            <?php
                            if ($usuario->is_ativo == 1) {
                                ?>
                                <a class="btn btn-outline-danger"
                                   href="<?php echo $this->getRelativeURL('desativar') . '/' . $usuario->id_pessoa; ?>">Desativar</a>
                                <?php
                            } else {
                                ?>
                                <a class="btn btn-outline-success"
                                   href="<?php echo $this->getRelativeURL('ativar') . '/' . $usuario->id_pessoa; ?>">Ativar</a>
                                <?php
                            }
                            ?>
                        </div>
                    </div>

                    <br/>
                </div>
                <?php
            }
            ?>
        </div>
    </div>

    <div class="tab-pane fade" id="cadastrarUsuarios" role="tabpanel" aria-labelledby="cadastrarUsuariosTab">
        <br/>

        <div class="card">
            <div class="card-body">
                <div class="offset-md-3 col-md-6 offset-lg-4 col-lg-4">
                    <form action="<?php echo $this->printRelativeURL('cadastrar'); ?>" method="post">
                        <div class="form-group">
                            <label for="nome">Nome</label>

                            <input required type="text" class="form-control" name="nome" id="nome"
                                   placeholder="Insira um nome completo">
                        </div>

                        <div class="form-group">
                            <label for="cpf">CPF</label>

                            <input required type="text" class="form-control" name="cpf" id="cpf"
                                   placeholder="607.674.070-16">
                        </div>

                        <div class="form-group">
                            <label for="nome_usuario">Nome de usuário</label>

                            <input required type="text" class="form-control" name="nome_usuario" id="nome_usuario"
                                   placeholder="Insira um nome de usuário">
                        </div>

                        <div class="form-group">
                            <label for="email">Email</label>

                            <input required type="email" class="form-control" name="email" id="email"
                                   placeholder="nome@exemplo.com">
                        </div>

                        <div class="form-group">
                            <label>Função</label>

                            <br/>

                            <div class="form-check form-check-inline">
                                <input class="form-check-input" type="radio" name="funcao" id="opcaoCoordenador"
                                       value="C">
                                <label class="form-check-label" for="opcaoCoordenador">Coordenador(a)</label>
                            </div>

                            <div class="form-check form-check-inline">
                                <input class="form-check-input" type="radio" name="funcao" id="opcaoOficial" value="O">
                                <label class="form-check-label" for="opcaoOficial">Oficial de promotoria</label>
                            </div>
                        </div>

                        <button type="submit" class="btn btn-outline-dark">Cadastrar</button>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>
